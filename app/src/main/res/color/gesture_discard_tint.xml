<?xml version="1.0" encoding="utf-8"?>
<!-- Symbol im Verwerfen-Ziel: eingerastet auf loom_errorContainer, sonst auf
     loom_surfaceContainerHigh — beide Paare sind auf Kontrast gerechnet (UX-Spec §5.6). -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_activated="true" android:color="@color/loom_onErrorContainer" />
    <item android:color="@color/loom_error" />
</selector>
