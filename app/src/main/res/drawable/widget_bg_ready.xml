<?xml version="1.0" encoding="utf-8"?>
<!-- Widget-Flaeche, Zustand "bereit". Farben wie der schwebende Knopf (UX-Spec §5.1),
     Ecken 24 dp wie die Standard-Widget-Form. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/loom_surfaceContainer" />
    <corners android:radius="24dp" />
    <stroke android:width="1dp" android:color="@color/loom_outlineVariant" />
</shape>
